spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    dynamic:
      primary: mysql
      strict: false
      datasource:
        mysql:
          username: ads
          password: 3eNAojt5VrqSEQUi38CIRMx0Pzu0LLh7
          driver-class-name: com.mysql.cj.jdbc.Driver
          url: jdbc:mysql://172.31.33.72:3306/ads?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8&useSSL=false
          druid:
            initial-size: 10
            min-idle: 10
            max-active: 500
            max-wait: 60000
            test-on-borrow: false
            test-on-return: false
            test-while-idle: true
            time-between-eviction-runs-millis: 60000
            min-evictable-idle-time-millis: 30000
            validation-query: select 'x'
            max-pool-prepared-statement-per-connection-size: 20
            filters: config,stat
  data:
    redis:
      sentinel:
        master: redis1
        nodes:
          - 172.31.41.156:16379
          - 172.31.46.109:16379
          - 172.31.41.23:16379
      jedis:
        pool:
          max-active: 1000  # 连接池最大连接数（使用负值表示没有限制）
          max-wait: -1ms      # 连接池最大阻塞等待时间（使用负值表示没有限制）
          max-idle: 10      # 连接池中的最大空闲连接
          min-idle: 5       # 连接池中的最小空闲连接

co-driver:
  rabbitmq:
    host: mq1.ronoinfo.com
    port: 5672
    username: max
    password: XzP9ZYrchRvrnpeVGZ1LJBeeZwkVSBIPZ
    virtualHost: /maxhost

