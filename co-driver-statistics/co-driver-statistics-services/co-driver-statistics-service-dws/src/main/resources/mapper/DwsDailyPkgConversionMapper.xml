<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.leo.ad.codriver.dws.dao.DwsDailyPkgConversionMapper">
    <delete id="deleteByDates">
        delete
        from dws_daily_pkg_conversion
        where dates = #{dates}
    </delete>
    <select id="statisticsActiveList" resultType="com.leo.ad.codriver.dws.entity.DwsDailyPkgConversion">
        select dates, pkg, 0 user_type, extra_type source_type, count(DISTINCT user_id) user_num
        from dwd_user_conversion
        where dates = #{dates}
        group by dates, pkg, extra_type
    </select>
    <select id="statisticsNewList" resultType="com.leo.ad.codriver.dws.entity.DwsDailyPkgConversion">
        select dates, pkg, 1 user_type, extra_type source_type, count(DISTINCT user_id) user_num
        from dwd_user_conversion
        where dates = #{dates}
          and dates = register_dates
        group by dates, pkg, extra_type
    </select>
</mapper>
