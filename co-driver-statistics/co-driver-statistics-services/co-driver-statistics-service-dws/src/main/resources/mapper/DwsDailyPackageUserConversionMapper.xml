<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.leo.ad.codriver.dws.dao.DwsDailyPackageUserConversionMapper">
    <delete id="deleteByDates">
        delete
        from dws_daily_package_user_conversion
        where dates = #{dates}
    </delete>
    <insert id="syncActiveList">
        insert into dws_daily_package_user_conversion(dates, pkg, country, `user_type`, `version`, user_num)
        select dates, pkg, country, 0, version, count(user_id)
        from dwd_user_conversion
        where dates = #{dates}
        group by dates, pkg, country, version
    </insert>
    <insert id="syncNewList">
        insert into dws_daily_package_user_conversion(dates, pkg, country, `user_type`, `version`, user_num)
        select dates, pkg, country, 1, version, count(user_id)
        from dwd_user_conversion
        where dates = #{dates}
          and dates = register_dates
        group by dates, pkg, country, version
    </insert>

    <select id="query" resultType="com.leo.ad.codriver.dws.entity.DwsDailyPackageUserConversion">
        select id,
               dates,
               pkg,
               country,
               version,
               user_type,
               user_num
        from dws_daily_package_user_conversion
        where dates = #{dates}
          and pkg = #{pkg}
          and version = #{version}
          and country = #{country}
          and user_type = 0
    </select>

</mapper>
